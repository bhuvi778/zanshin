import {Link} from 'react-router-dom';
import {useStore} from '../context/StoreContext';
import {money} from '../data';
import {SaveFragrance} from './ShopUtilities';
import LabelPack from '../components/LabelPack';
import {EditorialIntro,MomentInvitation} from './EditorialPages';
export default function Collection(){const {products}=useStore();return <div className="editorial-page"><EditorialIntro eyebrow="Explore Zanshin" title={<>Four intentions.<br/><em>Entirely yours.</em></>} description="Begin with a feeling. Discover four fragrances, each with its own way of meeting the moment."/><section className="ep-width ep-collection">{products.map((p,i)=><article key={p.slug} style={{'--sku':p.color}}><Link className="ep-pack" to={`/collection/${p.slug}`} aria-label={`Explore ${p.name}`}><LabelPack product={p}/></Link><div className="ep-product-copy"><span className="ep-kicker">0{i+1} / {p.size} / Eau de Parfum</span><h2><Link to={`/collection/${p.slug}`}>{p.name}</Link></h2><p className="ep-descriptor">{p.descriptor}</p><p>{p.mood}</p><p className="ep-price">{money(p.price)} <small>Sample price</small></p><div className="ep-product-actions"><Link className="ep-button" to={`/collection/${p.slug}`}>Explore {p.name} &rarr;</Link><SaveFragrance slug={p.slug} name={p.name}/></div></div></article>)}</section><p className="ep-width ep-caption">Bottle and carton presentation concepts. Prices are illustrative.</p><MomentInvitation/></div>}
