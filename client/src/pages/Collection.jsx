import {SaveFragrance} from './ShopUtilities';
import {Link} from 'react-router-dom';
import {useStore} from '../context/StoreContext';
import {money} from '../data';
import {PageHero,ProductVisual} from '../components/UI';
export default function Collection(){const {products,add}=useStore();return <><PageHero kicker="The collection / 50 ml eau de parfum" title="Choose how you<br><em>show up.</em>" description="Four fragrance identities, each made for a different energy."/><section className="shop-page"><div className="container shop-note"><span>01 â€” 04 / The collection</span><p>Prices are illustrative sample data.</p></div><div className="container shop-grid">{products.map((p,i)=><article className="shop-card" key={p.slug} style={{'--accent':p.color}}><ProductVisual product={p} index={i}/><div className="shop-card-content"><span className="eyebrow">0{i+1} / {p.mood}</span><h2>{p.name}</h2><SaveFragrance slug={p.slug} name={p.name}/><div className="product-tagline">{p.descriptor}</div><p>{p.story}</p><div className="shop-card-moments"><strong>For moments like</strong><span>{p.moments.join(' Â· ')}</span></div><div className="shop-card-meta"><span>Eau de Parfum Â· {p.size}</span><strong>{money(p.price)} <small>Demo price</small></strong></div><div className="shop-card-buy"><Link className="light-button" to={`/collection/${p.slug}`}>View details <span>â†—</span></Link><button className="dark-button" onClick={()=>add(p.slug)}>Add to bag <span>â†—</span></button></div></div></article>)}</div></section></>}
