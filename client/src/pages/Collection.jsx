import {Link} from 'react-router-dom';
import {useStore} from '../context/StoreContext';
import {money} from '../data';
import {SaveFragrance} from './ShopUtilities';
import ProductPack from '../components/ProductPack';
export default function Collection(){const {products}=useStore();return <section className="collection-editorial quiet-section"><span className="eyebrow">The Zanshin collection</span><h1>Begin with a feeling.<br/><em>Find your fragrance.</em></h1><p>Four identities. Four ways to meet the moment. Explore the world behind each fragrance.</p><div className="collection-editorial-grid">{products.map((p,i)=><article style={{'--sku':p.color}} key={p.slug}><Link to={`/collection/${p.slug}`}><ProductPack product={p} index={i}/></Link><div className="collection-caption"><span className="eyebrow">{p.descriptor}</span><h2><Link to={`/collection/${p.slug}`}>{p.name}</Link></h2><p>{p.mood}</p><p>{money(p.price)} <small>Demo price / {p.size}</small></p><div><Link className="quiet-link" to={`/collection/${p.slug}`}>Explore {p.name} &rarr;</Link><SaveFragrance slug={p.slug} name={p.name}/></div></div></article>)}</div><p className="pack-note">Bottle and carton presentation concepts. Prices are illustrative.</p></section>}
